# 生产环境配置模板
#
# 用法：复制为 server/.env 后逐项填写（.env 已被 git 忽略，不会入库）。
#   cp server/.env.production.example server/.env
# npm start 会自动加载 server/.env；Docker / 进程管理器部署时改为注入系统环境变量即可，无需本文件。

NODE_ENV=production

# JWT 签名密钥，生产必填。生成示例：
#   node -e "console.log(require('node:crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# 后端监听端口（默认 3000）
# PORT=3000
# SQLite 数据库文件路径（默认 server/data/lab-checkin.db；目录需已存在）
# DB_PATH=
# 允许的前端来源，多个用英文逗号分隔；不设置则放行所有来源（生产建议显式设置）
# CORS_ORIGIN=https://lab.example.com
# 经 Nginx 等反向代理部署时设为 true（或填代理跳数），限流按真实客户端 IP 计数
# TRUST_PROXY=true
# 首次启动建好管理员后建议设为 false，避免清空数据库后重建默认管理员
# CREATE_DEFAULT_ADMIN=false
# 首次建库时默认管理员 admin 的初始密码（默认 admin123，登录后应立即修改）
# ADMIN_PASSWORD=
# 业务时区：签到/签退时间与「今日」统计按服务器本地时间计算；容器内默认为 UTC（中国部署会偏移 8 小时），
# 裸机部署通常跟随系统时区无需设置，Docker 部署已在 docker-compose.yml 中默认注入 TZ=Asia/Shanghai
# TZ=Asia/Shanghai
